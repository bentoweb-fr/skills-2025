// const host = window.location.hostname;
// console.log(host);

import "@css/styles.scss";
// import "@/assets/js/scrollzoom.js";

import { createApp } from "vue";
import App from "./App.vue";
createApp(App).mount("#app");
