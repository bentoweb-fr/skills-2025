services:
  api:
    # container_name: skills2025_api
    image: ghcr.io/bentoweb-fr/skills2025/api
    pull_policy: never
    environment:
      PUID: 1000
      PGID: 1000
      APP_ENV: prod
      # MYSQL_DATABASE: ${MYSQL_DATABASE}
      # MYSQL_USER: ${MYSQL_USER}
      # MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      DATABASE_URL: "mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:3306/${MYSQL_DATABASE}?serverVersion=8.0.32&charset=utf8mb4"
  #   build:
  #     context: ./api
  #     dockerfile: ../docker/api/Dockerfile
    volumes:
      - ./api:/var/www/api
    networks:
      - bentoweb_network
    expose:
      - "9000"
    depends_on:
      - mysql

  # front:
  #   container_name: skills2025_front
    # image: skills2025/front:latest
  #   build:
  #     context: ./front
  #     dockerfile: ../docker/front/Dockerfile
  #   volumes:
  #     - ./front:/var/www/front
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     NODE_ENV: prod
  # #     VITE_API_URL: https://api.skills2025.local
  # #     SHELL: /bin/ash # Fix chokidar-cli error
  #   restart: unless-stopped
  #   networks:
  #     - bentoweb_network
  #   ports:
  #     - "3000:3000"

  nginx:
    image: ghcr.io/bentoweb-fr/skills2025-nginx
    pull_policy: never
    # image: ghcr.io/${REGISTRY_USER}/skills2025-nginx:latest
    # container_name: skills2025_nginx
    volumes:
      #   - ./api:/var/www/api
      - /var/www/front:/var/www/front
      - ./nginx:/etc/nginx
    networks:
      - bentoweb_network
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
      # - front

  mysql:
    image: mysql:8.0
    restart: always
    env_file:
      - .env
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - bentoweb_db_data:/var/lib/mysql

# phpmyadmin:
#   image: phpmyadmin:5.2.1
#   container_name: skills2025_phpmyadmin
#   restart: always
#   ports:
#     - 8022:80
#   links:
#     - mysql:mysql
#   environment:
#     PMA_HOST: mysql
#   depends_on:
#     - mysql
#   networks:
#     - bentoweb_network

networks:
  bentoweb_network:
    driver: bridge

volumes:
  bentoweb_db_data:
    external: true
    name: bentoweb_db_data